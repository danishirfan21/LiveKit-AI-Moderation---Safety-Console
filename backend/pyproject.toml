[project]
name = "livekit-moderation-backend"
version = "0.1.0"
description = "LiveKit AI Moderation & Safety Console Backend"
requires-python = ">=3.10"

[tool.ruff]
line-length = 100
target-version = "py310"
